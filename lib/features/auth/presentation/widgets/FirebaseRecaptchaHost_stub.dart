import 'package:flutter/material.dart';

class FirebaseRecaptchaHost extends StatelessWidget {
  const FirebaseRecaptchaHost({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
